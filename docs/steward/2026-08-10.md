# Steward — 2026-08-10

## הכי חשוב עכשיו

CI על `main` אדום **15 יום ברציפות** (מ-26.07). הפתרון עדיין יושב ב-**PR #9** — ירוק, `mergeable_state: clean` מול `main` הנוכחי (עדיין ב-78a7689, לא זז מ-28.07). זהו הדיווח הרביעי ברציפות על אותה תקלה בדיוק (#8 → #10 → #11 → כאן). לא בוצע שום מיזוג בשבוע האחרון. החוסם הוא החלטת מיזוג, לא קוד.

## מה תיקנתי

לא בוצע שינוי קוד חדש. אימתתי מחדש את כל השרשרת:

- `main` לא זז מ-78a7689 (28.07).
- שיבוטתי את הענף, הרצתי בדיקות בפועל: backend `pnpm build` ✅, `pnpm lint` (לא נבדק בנפרד — build עבר), `pnpm test` ❌ (1/6 חבילות נכשלות — `brief-renderer.spec.ts` נופל על `import puppeteer` ב-`website-extractor.service.ts`, חבילת ESM ש-Jest לא מפרסר). frontend `pnpm lint` ❌ (`ContactDetail.tsx:89`, `react-hooks/set-state-in-effect` — קריאת `setState` בתוך `useEffect` על טעינת כרטיס איש קשר).
- שני הכשלים האלה **כבר מתוקנים ומאומתים ירוקים** ב-PR #9 (מ-05.08): extraction types לקובץ נטול-puppeteer, והעברת `ContactDetail` לתבנית ה-React הרשמית ("adjusting state when a prop changes"). לא פתחתי PR מתחרה/כפול על תיקון שכבר קיים ומאושר — זה רק יגדיל את מלאי ה-PR-ים הפתוחים, שזו בעצמה הבעיה.
- לא נמצא כשל CI/build/lint חדש שלא דווח כבר.

## דורש החלטה

1. **למזג את PR #9** — דחוף. ירוק, נקי, פותר את ה-CI האדום מיידית. אין סיבה טכנית לא למזג; זו רק פעולה שמחכה ללחיצה.
2. **7 PR-ים פתוחים ולא ממוזגים** (#4, #5, #6, #7, #8, #9, #10, #11 — כולם עדיין open, כולם מבוססים על אותו `main` ישן): לאחר מיזוג #9, ל-#4/#5/#7/#11 אין תוכן ייחודי (משוכפלים לגמרי). אפשרויות: (א) לסגור אותם עכשיו בלי לקרוא, (ב) לבדוק בקצרה אם משהו ב-#6/#8 שווה הצלה (הסרת קוד מת + תיקון אייקון RTL ב-#6; עדכוני `socket.io-parser`/`@modelcontextprotocol/sdk` ב-#8) ואז לסגור את כל השאר.
3. **תזמון ה-heartbeat** — הריצה היומית ממשיכה לפתוח PR-ים חדשים על גבי אותו `main` שלא זז, מה שמייצר מלאי גדל של דיווחים כפולים על אותה תקלה. אחרי שיטופל הסעיף הקודם, שווה לשקול: לעצור את ה-heartbeat היומי עד שהחוסם יטופל, או לשנות אותו כך שלא יפתח PR חדש אם אין שינוי מהדיווח הקודם.

## בהמתנה

- `react-router` v8 — חולשת CSRF ב-RSC Mode (high), שדרוג מייג'ור, לא נבדק.
- 22 ממצאי `pnpm audit` ב-`@modelcontextprotocol/sdk` (backend, כולם transitive).
- אין קוד חדש שנבדק מעבר לאימות מחדש של המצב הידוע.

---

**אימות:** אין שינוי קוד בענף הזה — רק דוח. אומת ידנית מחדש: backend `pnpm install --frozen-lockfile` ✅, `pnpm build` ✅, `pnpm test` ❌ (כשל ידוע, מתוקן ב-PR #9). frontend `pnpm install --frozen-lockfile` ✅, `pnpm lint` ❌ (כשל ידוע, מתוקן ב-PR #9). הסטטוס הרלוונטי הוא של PR #9 (ירוק ב-CI, `mergeable_state: clean`).
