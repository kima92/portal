# Steward — 2026-08-13

## הכי חשוב עכשיו

**11 PR-ים פתוחים של ה-Steward (#4 עד #14, מ-31.7 עד 12.8) לא נסקרו ולא מוזגו.** זו התקלה המשמעותית ביותר שנמצאה היום — לא בקוד, אלא בתהליך עצמו: ריצה יומית שמייצרת PR ואף אחד לא ממזג אותו לא ממלאת את התפקיד שלה. לא בוצע תיקון קוד נוסף היום כדי לא לשכפל עבודה שכבר קיימת ומאומתת ב-PR #14 הפתוח.

## מה בדקתי (בלי לשנות קוד)

הרצתי מחדש `build`/`lint`/`test` על `main` הנוכחי (ללא המיזוגים הממתינים) כדי לוודא שהממצאים של PR #14 עדיין רלוונטיים:

- **frontend lint נכשל**: `ContactDetail.tsx` — `react-hooks/set-state-in-effect` (עדכון state סינכרוני בתוך `useEffect`).
- **backend test suite נכשל**: `brief-renderer.spec.ts` נופל כי `website-extractor.service.ts` מייבא `puppeteer` (ESM-only) ו-`ts-jest` לא מתמלל אותו.
- **backend build**: ✅ עובר. **frontend build**: ✅ עובר. **backend lint**: ✅ עובר.

שני הכשלים האלה **כבר מתוקנים ומאומתים** ב-PR #14 (וגם bump ל-`multer` 2.1.1→2.2.0 שסוגר חולשת DoS בדרגת high). אין טעם לפתוח תיקון מקביל/כפול כל עוד #14 מחכה למיזוג.

## דורש החלטה

1. **מה עושים עם 11 ה-PR-ים הפתוחים?** ההמלצה שלי: למזג את **#14** (העדכני והמקיף ביותר — כולל את כל התיקונים הבטוחים שנצברו), ואז לסגור את **#4–#13** כ-superseded (כולם ענפו מאותו commit בסיס, ולא זזו מאז — סביר שרובם חופפים או מיושנים ביחס ל-#14). אם יש בהם ממצא ספציפי שרוצים לשמר, כדאי לעבור עליהם באלכסון לפני הסגירה.
   - אופציה ב': למזג את כולם לפי הסדר הכרונולוגי (סיכון לקונפליקטים בין 11 ענפים שלא זזו יחד).
   - אופציה ג': לבטל את ריצת ה-heartbeat היומית עד שהתור הקיים ייסגר, כדי לא להוסיף עוד PR-ים על הערימה.
2. **פערים מ-PR #14 שעדיין פתוחים** (רלוונטי כשימוזג): חולשות אבטחה טרנזיטיביות (26 סה"כ, 9 high — `ws` דרך `@socket.io/redis-adapter`, `socket.io-parser`, `hono`/`fast-uri` דרך `@modelcontextprotocol/sdk`) דורשות `pnpm.overrides` עם בדיקת regression; ו-bump ל-`puppeteer` 25.2.0→25.6.0 שנוגע לקוד production (`website-extractor.service.ts`, `pdf-renderer.service.ts`) ודורש בדיקה ידנית.

## בהמתנה

- TODO ישן ב-`widget.controller.ts:20` — CORS/Origin לא אוכף `Business.widgetAllowedOrigins` עדיין.
- `console.log` בודד ב-`main.ts:79` במקום Logger.
- מספר bump-ים מינוריים זמינים ב-backend/frontend (nestjs packages, pg, prettier, react, vite ועוד) — ללא CVE, לא דחוף.
- כדאי לשקול לצמצם את תדירות ה-heartbeat (למשל שבועי במקום יומי) כל עוד אין קיבולת לסקור PR ביום.

---

**אימות**: `backend` — `pnpm build` ✅, `pnpm lint` ✅, `pnpm test` ❌ (כשל קיים, מתוקן ב-#14, לא שוכפל כאן). `frontend` — `pnpm build` ✅, `pnpm lint` ❌ (כשל קיים, מתוקן ב-#14, לא שוכפל כאן). לא בוצע שינוי קוד בריצה זו.
