# Steward — 2026-08-25

## הכי חשוב עכשיו

CI על `main` אדום **31 יום ברציפות** (מ-25.07, ריצה #24 — הפעם האחרונה שהיה ירוק). התיקון המלא יושב מאומת וירוק ב-PR מאז 05.08 (PR #9), ומאז נערמו עוד PR-ים של Steward יום אחרי יום — **19 PR-ים פתוחים, אף אחד לא מוזג מעולם**. `main` עצמו לא זז מ-78a7689 (28.07). זו כבר הפעם השנייה שדוח Steward מצביע במפורש על זה (PR #12, לפני שבועיים, הציע בדיוק את אותה המלצה) — החוסם הוא החלטת מיזוג שלך, לא קוד.

**לא פתחתי היום עוד PR-תיקון עשרים-במספר.** בדקתי: התיקונים הקריטיים (build/lint/test שבורים) כבר קיימים ומאומתים ב-PR #23 (24.08, העדכני והמקיף ביותר). אבל **לא כל התיקונים המצטברים נמצאים שם** — לדוגמה, PR #18 (16.08) תיקן חצים ליטרליים (`←`/`→`) ב-`Automations.tsx` ו-`WhatsappSettings.tsx` (הפרה של כלל האייקונים ב-CLAUDE.md), ותיקון הזה **לא** ב-#23. וידאתי ישירות מול ה-working tree: `Files.tsx` עדיין מכיל חץ ליטרלי ולא תוקן באף PR. כלומר: 19 ה-PR-ים לא זהים זה לזה — יש בהם עבודה אמיתית ומפוזרת, ולא ניתן פשוט למזג את האחרון ולסגור את השאר בלי לאבד תיקונים.

## מה עשיתי

- לא בוצע שינוי קוד. אימתתי מחדש את מצב `main`: `git log`/`git status` מראה `HEAD` עדיין ב-78a7689, בדיוק כמו לפני 4 שבועות.
- בדקתי דרך GitHub API את 19 ה-PR-ים הפתוחים: כולם `mergeable_state: clean`, כולם CI ירוק (נבדק על PR #23 — 3/3 checks עברו).
- השוויתי תוכן בין כמה PR-ים לדוגמה (#12 — דוח בלבד; #18 — תיקוני RTL אמיתיים; #23 — build/test/lint) כדי לוודא שההמלצה למטה מדויקת ולא מניחה כפילות שלא קיימת.

## דורש החלטה

1. **למזג את PR #23 עכשיו.** ירוק, `mergeable_state: clean`, פותר את שלושת שערי האימות שהיו שבורים בו-זמנית (כולל תקלה חמורה: build יכול היה לפלוט `dist/` ריק עם `exit code 0`). אין סיבה טכנית לחכות.
2. **מה לעשות עם 18 ה-PR-ים הנותרים.** יש בהם עבודה לא-כפולה (למשל תיקוני חצים ליטרליים ב-#18). שלוש אפשרויות:
   - **(א)** לעבור עליהם בעצמי (Steward) בסבב ייעודי — לזהות כל תיקון ייחודי שלא ב-#23, לאחד לענף אחד נקי ומאומת, לפתוח PR מאוחד יחיד, ולסגור את כל ה-19 הישנים כ-superseded. הכי יסודי, דורש סבב מלא.
   - **(ב)** לסגור את כל ה-18 הישנים עכשיו בלי בדיקה נוספת ולקבל שהתיקונים הלא-ב-#23 (כמו חצי ה-RTL) ילכו לאיבוד עד שיתגלו מחדש — הכי מהיר, אבל מבזבז עבודה שכבר נעשתה ואומתה.
   - **(ג)** לעצור את ריצת ה-heartbeat היומית עד שהסבב הזה ייסגר, כדי שהערימה תפסיק לגדול בזמן שמחליטים.
3. **תזמון ה-heartbeat עצמו** — כפי שכבר עלה ב-PR #12: כרגע כל ריצה יומית פותחת PR חדש על גבי אותו `main` שלא זז מאז 28.07, מה שממשיך להגדיל את הערימה. שווה לשקול לשנות את הריצה כך שתדלג על פתיחת PR חדש כשאין שינוי מהותי מהדיווח הקודם — כפי שנעשה כאן.

## בהמתנה

- `Files.tsx` — עדיין מכיל חץ ליטרלי (`←`/`→`) שלא תוקן באף PR קיים.
- תלויות בקאנד בפיגור מינורי (~19 חבילות, `@nestjs/*` וכו') — פורט ב-PR #23, ממתין להחלטת עדכון נפרדת.
- `pnpm audit --prod` בבקאנד — ~16 ממצאים, רובם טרנזיטיביים; פורט לראשונה ב-PR #4.
- `widget.controller.ts` — הערת TODO מיושנת על `widgetAllowedOrigins` (כבר תוקנה בהערה ב-PR #4, ייתכן שדורשת בדיקה שהתיקון בפועל קיים ב-`main`).
- אין ראנר טסטים בפרונט (`make test` מריץ רק eslint) — פער כיסוי מובנה, חוזר בכמה דוחות.

---

## פלט האימות

אין שינוי קוד בענף הזה — דוח בלבד. אומת מול ה-working tree הנוכחי (= `main` @ 78a7689):

```
git log --oneline -1        → 78a7689 (ללא שינוי מ-28.07)
git status                  → נקי
grep חצים ליטרליים בפרונט   → נמצאו ב-3 קבצים: WhatsappSettings.tsx, Files.tsx, Automations.tsx
                               (הראשונים השניים כבר תוקנו ב-PR #18, אך לא מוזגו)
```

מצב ה-CI וה-mergeability של PR #23 אומת ישירות מול GitHub API: `mergeable_state: clean`, 3/3 checks (`docker`, `backend`, `frontend`) — `success`.

---
_Generated by [Claude Code](https://claude.ai/code/session_01V6wpL1qBSe7KAduYZjA1zz)_
